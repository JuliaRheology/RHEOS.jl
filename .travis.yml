## Documentation: http://docs.travis-ci.com/user/languages/julia/
language: julia

os:
  - linux
  # - osx

julia:
  # - 0.7
  - 1.0
  # - nightly

notifications:
  email: false

# after_success:
  # - julia --project=coverage/ -e 'using Pkg; Pkg.instantiate()'
  # - julia --project=coverage/ coverage/coverage.jl

jobs:
  include:
    - stage: "Documentation"
      julia: 1.0
      os: linux
      script:
        - julia -e 'using Pkg; Pkg.test("RHEOS"; coverage=true)'
        - julia --project=docs/ -e 'using Pkg; Pkg.instantiate(); Pkg.develop(PackageSpec(path=pwd()))'
        - julia --project=docs/ docs/make.jl
        # - julia -e 'using Pkg; cd(Pkg.dir("RHEOS")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
# after_success: skip